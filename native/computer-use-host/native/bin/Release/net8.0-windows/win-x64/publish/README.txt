Placeholder: the Windows computer-use host (deyin-computer-use-host.exe) is not
included in this local build. Build it with:
  cd packages/computer-use-host/native && dotnet publish -c Release -r win-x64
Computer use stays disabled by default (Settings -> Computer Use).
