{
  "name": "@deyin/native-core",
  "version": "0.1.0",
  "description": "Deyin's in-house native hot path: SSE framing, token counting, wire compression, parallel grep. Pure Rust, napi glue only.",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "MIT",
  "files": ["index.js", "index.d.ts", "*.node"],
  "scripts": {
    "build": "node scripts/build.js",
    "build:debug": "node scripts/build.js --debug"
  }
}
