/**
 * @deyin/ui public entry. Apps import { App } from "@deyin/ui" and mount it
 * after installing their transport on window.deyin; styles live at
 * "@deyin/ui/styles.css".
 */
export { App } from "./app.js";
