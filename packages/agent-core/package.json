{
  "name": "@deyin/agent-core",
  "version": "0.1.1",
  "description": "Deyin's agentic runtime: OpenAI-compatible streaming with tool_calls accumulation, the agent loop, built-in tools (bash, read, write, edit, grep, glob, ls, websearch, todo), the permission engine, session persistence, layered config, prompts/compaction, and an MCP client. Consumed by the CLI today and the desktop app later.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DeYinAI/deyin-desktop.git",
    "directory": "packages/agent-core"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@deyin/host-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
