/** Re-export shared repo workflow from host-core. */
export {
  RepoManager,
  compareUrlFor,
  sanitizeRemoteUrl,
  slugifyRepo,
  type RepoConnectOptions,
  type RepoState,
  type RepoShipResult,
  type RepoProgressStage,
} from "@deyin/host-core";
